name: Create envfile

on: [push]

jobs:

  create-envfile:
 
    runs-on: ubuntu-18.04
 
    steps:
    - name: Make envfile
      uses: SpicyPizza/create-envfile@v1
      with:
        GIT: ${{ secrets.GIT_TOKEN }}
        directory: assets/
        file_name: .env
